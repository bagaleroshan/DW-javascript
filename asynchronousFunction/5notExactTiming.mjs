//does setTimeOut guarantee  the exact time.
//setTime Guarantee the  minimum time execute that the code gets executed but does not guarantee the executed time.
//guarantee min time that the function will execute but doesn't guarantee the executed time.
